### Git Add

`git add nombre_archivo`
Agrega un archivo a la zona de preparación (stage).

`git add .`
Agrega todos archivo a la zona de preparación (stage), los estemos siguiendo o no.

`git add -A`
Agrega todos archivo a la zona de preparación (stage) que estemos siguiendo.
